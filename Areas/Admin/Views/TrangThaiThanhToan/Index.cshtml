@model IEnumerable<DACS.Models.TrangThaiThanhToan>

@{
    ViewData["Title"] = "Index";
    Layout = "_DashBoardAdmin";

}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenTrangThaiThanhToan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenTrangThaiThanhToan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaTrangThaiThanhToan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaTrangThaiThanhToan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaTrangThaiThanhToan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
