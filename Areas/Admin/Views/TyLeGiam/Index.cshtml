@model IEnumerable<DACS.Models.TyLeGiam>

@{
    ViewData["Title"] = "Index";
    Layout = "_DashBoardAdmin";
}
<h1>Index</h1>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Description Reduction rate
            </th>
            <th>
                Reduction rate
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MoTaTyLeGiam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhanTramGiam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaTyLeGiam">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaTyLeGiam">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaTyLeGiam">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
